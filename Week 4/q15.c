// Count the number of internal (non-leaf) nodes in a BSTree

#include <stdio.h>

struct node {
	int value;
	struct node *left;
	struct node *right;
};

int BSTreeCountInternal(struct node *t) {
    // TODO
    return 42;
}
