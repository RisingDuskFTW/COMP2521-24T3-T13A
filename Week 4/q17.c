// Find the level that a given value is in the tree

#include <stdio.h>

struct node {
	int value;
	struct node *left;
	struct node *right;
};

int BSTNodeLevel(struct node *t, int key) {
    // TODO
    return 42;
}
